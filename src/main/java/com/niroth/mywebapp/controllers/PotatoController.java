package com.niroth.mywebapp.controllers;

import org.springframework.stereotype.Controller;

//because it is the controller, so we use annotation @Controller
//This kind of annotation is to tell springboot to create a bean if this class for u, so u can inject it next time without writing Controller c = new Controller()
@Controller
public class PotatoController {
}
